using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Sc_AINavigationMesh : MonoBehaviour
{
    /*private NavMeshAgent navMeshAgent;

    private Transform movePositionTransfrom;
    private string currentState;

    [SerializeField]
    private Sc_PatrolState patrolState;

    private void Awake()
    {
        navMeshAgent = GetComponent<NavMeshAgent>();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }

    public void AIMovement(Transform movePositionTransfrom, string state)
    {
        this.movePositionTransfrom = movePositionTransfrom;
        currentState = state;
    }

    IEnumerator Waiting()
    {
        yield return new WaitForSeconds(0.75f);
        yield return null;
    }*/
}
